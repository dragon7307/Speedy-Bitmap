# Speedy-Bitmap

The default C# Bitmap is <b>slow</b > - very <b>slow! </b>    <br> 
But luckily, this implementation of a C# Bitma,p fixes that.    <br>  
Speedy-Bitmap is a  highly performant bitmap making use of unsafe code to achieve  up to 8 times faster  rading and writing of pixels 
  
